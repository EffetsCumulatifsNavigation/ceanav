### Sites d’intérêt culturels, patrimoniaux et archéologiques

```{r site_setup, echo = FALSE}
meta <- read_yaml("../data/data-metadata/int_cv_site.yml")
```

(Tableau \@ref(tab:siteCat))

<br/>

```{r siteCat}
tab <- as.data.frame(meta$dataDescription$categories)
tab <- arrange(tab, francais)
tab <- select(tab, francais)


tab %>%
  knitr::kable(
    col.names = c("Catégories de sites"),
    row.names = FALSE,
    caption = "Catégories de sites d'intérêt culturels, patrimoniaux et archéologiques identifiés pour le Saint-Laurent et le Saguenay."
  )
```

<br/>



<!--
#### Kahnawake

TODO: Vérifier la catégorie traffic identifiée par la communauté. Est-ce qu'on devrait l'ajouter aux stresseurs de navigation?
TODO: Même chose avec la seigneurie
-->
